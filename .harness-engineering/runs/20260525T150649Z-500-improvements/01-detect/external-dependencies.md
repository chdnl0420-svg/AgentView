# External dependencies

UI-only feature-add. 외부 의존성 없음.

- 호출되는 IPC: 모두 main 프로세스 자체 (window.av.sessions.*, window.av.shell.openPath, navigator.clipboard).
- production credential / base URL: 없음.
- BLOCKED 사유: 없음.

진행 OK.
